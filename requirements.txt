# # Core PyTorch and deep learning
# torch>=2.0.0
# torchvision>=0.15.0

# Medical imaging and MONAI
dicom2nifti
monai[all]
monai-generative
nibabel

# Data processing
pandas
numpy>=1.24.0,<2
h5py
SimpleITK
scikit-image

# Visualization and logging
matplotlib
tqdm
Pillow>=10.0.0
dominate>=2.8.0
wandb>=0.16.0

# Utilities
einops
argparse
scipy
torchmetrics[image]
torch-fidelity
# pyradiomics is installed separately in the Dockerfile before other deps
# to work around its broken PyPI packaging (pins PyWavelets<=1.0.0 which
# fails to build on Python 3.11). See Dockerfile for install instructions.